import React from 'react';
import { Box } from '@mui/material';

const hudSx = { position: 'absolute', top: 0, right: 0, left: 0, bottom: 0 };
export const HUD = () => {
    return <Box sx={hudSx}>hello2</Box>;
};
